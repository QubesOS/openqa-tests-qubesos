Salt formula for installing updates in dom0 and all VMs, either from testing
repository or a custom one (configurable with pillars).

This directory is provided as salt_state_update.tgz in assets/other
(/var/lib/openqa/factory/other/) and used by tests/update.pm
